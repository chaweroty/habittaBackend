/**
 * @typedef {Object} Application
 * @property {number} id - ID único de la solicitud
 * @property {number} id_renter - ID del usuario que realiza la solicitud
 * @property {number} id_property - ID de la propiedad solicitada
 * @property {string} status - Estado de la solicitud (ej: pendiente, aprobada, rechazada)
 * @property {Date} application_date - Fecha en que se realizó la solicitud
 * @property {User} renter - Usuario que realiza la solicitud
 * @property {Property} property - Propiedad solicitada
 */

// Este archivo es solo para documentación y referencia de los campos del modelo Application.

module.exports = {};
